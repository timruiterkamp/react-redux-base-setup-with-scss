import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header className="App-header">
                <h1 className="App-title">React base setup with scss</h1>
            </header>
        );
    }
}

export default Header;